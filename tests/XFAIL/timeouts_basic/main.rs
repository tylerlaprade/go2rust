fn main() {
    let mut c1 = std::sync::Arc::new(std::sync::Mutex::new(Some(vec![std::sync::Arc::new(std::sync::Mutex::new(Some(0))); 1])));
    // TODO: Unhandled statement type: GoStmt
    // TODO: Unhandled statement type: SelectStmt
    let mut c2 = std::sync::Arc::new(std::sync::Mutex::new(Some(vec![std::sync::Arc::new(std::sync::Mutex::new(Some(0))); 1])));
    // TODO: Unhandled statement type: GoStmt
    // TODO: Unhandled statement type: SelectStmt
}