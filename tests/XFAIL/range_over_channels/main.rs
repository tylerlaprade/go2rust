use std::sync::{Arc, Mutex};

fn main() {
    let mut queue = ;
    // TODO: Unhandled statement type: SendStmt
    // TODO: Unhandled statement type: SendStmt
    (close.lock().unwrap().as_ref().unwrap())(queue.clone());

    for elem in 0..(*queue.lock().unwrap().as_mut().unwrap()).len() {
        println!("{}", elem);
    }
}