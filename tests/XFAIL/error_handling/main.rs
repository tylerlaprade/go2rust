pub fn divide(a: f64, b: f64) -> f64 {
    
    return a / b;
}

pub fn sqrt(x: f64) -> f64 {
    
    let mut result = x / 2;
    let mut i = 0;
    while i < 10 {
        result =  / 2;
        i += 1;
    }
    return result;
}

#[derive(Debug)]
struct CustomError {
    code: i32,
    message: String,
}

pub fn error() -> String {
    return fmt.sprintf("Error %d: %s".to_string(), e.code, e.message);
}

pub fn process_value(val: i32) -> error {
    
    
    return nil;
}

fn main() {
    let mut result, let mut err = divide(10, 2);
    
    result, err = divide(10, 0);
    
    let mut sqrtResult, let mut err = sqrt();
    
    err = process_value();
    
    err = process_value(150);
    
    err = process_value(50);
    
}