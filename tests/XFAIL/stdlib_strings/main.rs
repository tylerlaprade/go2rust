fn main() {
    println!("{}", "=== Basic string operations ===".to_string());
    let mut str = std::sync::Arc::new(std::sync::Mutex::new(Some("Hello, World!".to_string())));
    print!("Original string: {}\n", (*str.lock().unwrap().as_ref().unwrap()));
    print!("Length: {}\n", (*str.lock().unwrap().as_ref().unwrap()).len());
    print!("First character: %c\n", (*str.lock().unwrap().as_ref().unwrap())[0]);
    print!("Last character: %c\n", (*str.lock().unwrap().as_ref().unwrap())[(*str.lock().unwrap().as_ref().unwrap()).len() - 1]);
    print!("Substring [0:5]: {}\n", (*str.lock().unwrap().as_ref().unwrap())[0..5].to_vec());
    print!("Substring [7:]: {}\n", (*str.lock().unwrap().as_ref().unwrap())[7..].to_vec());
    println!("{}", "\n=== String concatenation ===".to_string());
    let mut first = std::sync::Arc::new(std::sync::Mutex::new(Some("Hello".to_string())));
    let mut second = std::sync::Arc::new(std::sync::Mutex::new(Some("World".to_string())));
    let mut combined = std::sync::Arc::new(std::sync::Mutex::new(Some((*first.lock().unwrap().as_ref().unwrap()) + ", ".to_string() + (*second.lock().unwrap().as_ref().unwrap()) + "!".to_string())));
    print!("Concatenated: {}\n", (*combined.lock().unwrap().as_ref().unwrap()));
    println!("{}", "\n=== strings package functions ===".to_string());
    let mut text = std::sync::Arc::new(std::sync::Mutex::new(Some("  Go is awesome for systems programming  ".to_string())));
    print!("Original: '{}'\n", (*text.lock().unwrap().as_ref().unwrap()));
    let mut trimmed = std::sync::Arc::new(std::sync::Mutex::new(Some((*text.lock().unwrap().as_ref().unwrap()).trim())));
    print!("Trimmed: '{}'\n", (*trimmed.lock().unwrap().as_ref().unwrap()));
    print!("Upper: {}\n", (*trimmed.lock().unwrap().as_ref().unwrap()).to_uppercase());
    print!("Lower: {}\n", (*trimmed.lock().unwrap().as_ref().unwrap()).to_lowercase());
    print!("Title: {}\n", (*strings.lock().unwrap().as_ref().unwrap()).title(std::sync::Arc::new(std::sync::Mutex::new(Some((*trimmed.lock().unwrap().as_ref().unwrap()))))));
    println!("{}", "\n=== String searching ===".to_string());
    let mut searchText = std::sync::Arc::new(std::sync::Mutex::new(Some("The quick brown fox jumps over the lazy dog".to_string())));
    print!("Text: {}\n", (*searchText.lock().unwrap().as_ref().unwrap()));
    print!("Contains 'fox': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).contains(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("fox".to_string())))));
    print!("Contains 'cat': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).contains(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("cat".to_string())))));
    print!("Index of 'fox': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).index(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("fox".to_string())))));
    print!("Index of 'cat': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).index(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("cat".to_string())))));
    print!("Last index of 'the': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).last_index(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("the".to_string())))));
    print!("Count of 'the': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).count(std::sync::Arc::new(std::sync::Mutex::new(Some((*searchText.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("the".to_string())))));
    println!("{}", "\n=== Prefixes and suffixes ===".to_string());
    let mut filename = std::sync::Arc::new(std::sync::Mutex::new(Some("document.txt".to_string())));
    print!("Filename: {}\n", (*filename.lock().unwrap().as_ref().unwrap()));
    print!("Has .txt suffix: {}\n", (*strings.lock().unwrap().as_ref().unwrap()).has_suffix(std::sync::Arc::new(std::sync::Mutex::new(Some((*filename.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some(".txt".to_string())))));
    print!("Has .pdf suffix: {}\n", (*strings.lock().unwrap().as_ref().unwrap()).has_suffix(std::sync::Arc::new(std::sync::Mutex::new(Some((*filename.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some(".pdf".to_string())))));
    print!("Has 'doc' prefix: {}\n", (*strings.lock().unwrap().as_ref().unwrap()).has_prefix(std::sync::Arc::new(std::sync::Mutex::new(Some((*filename.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("doc".to_string())))));
    println!("{}", "\n=== Splitting and joining ===".to_string());
    let mut csv = std::sync::Arc::new(std::sync::Mutex::new(Some("apple,banana,cherry,date".to_string())));
    print!("CSV: {}\n", (*csv.lock().unwrap().as_ref().unwrap()));
    let mut fruits = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).split(std::sync::Arc::new(std::sync::Mutex::new(Some((*csv.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some(",".to_string())))))));
    print!("Split result: {}\n", (*fruits.lock().unwrap().as_ref().unwrap()));
    let mut rejoined = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).join(std::sync::Arc::new(std::sync::Mutex::new(Some((*fruits.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some(" | ".to_string())))))));
    print!("Rejoined: {}\n", (*rejoined.lock().unwrap().as_ref().unwrap()));
    let mut sentence = std::sync::Arc::new(std::sync::Mutex::new(Some("The quick brown fox".to_string())));
    let mut words = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).fields(std::sync::Arc::new(std::sync::Mutex::new(Some((*sentence.lock().unwrap().as_ref().unwrap()))))))));
    print!("Words: {}\n", (*words.lock().unwrap().as_ref().unwrap()));
    println!("{}", "\n=== String replacement ===".to_string());
    let mut original = std::sync::Arc::new(std::sync::Mutex::new(Some("I like cats and cats like me".to_string())));
    print!("Original: {}\n", (*original.lock().unwrap().as_ref().unwrap()));
    let mut replaced = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).replace(std::sync::Arc::new(std::sync::Mutex::new(Some((*original.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("cats".to_string()))), std::sync::Arc::new(std::sync::Mutex::new(Some("dogs".to_string()))), std::sync::Arc::new(std::sync::Mutex::new(Some(1)))))));
    print!("Replace first 'cats': {}\n", (*replaced.lock().unwrap().as_ref().unwrap()));
    let mut replacedAll = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).replace_all(std::sync::Arc::new(std::sync::Mutex::new(Some((*original.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("cats".to_string()))), std::sync::Arc::new(std::sync::Mutex::new(Some("dogs".to_string())))))));
    print!("Replace all 'cats': {}\n", (*replacedAll.lock().unwrap().as_ref().unwrap()));
    println!("{}", "\n=== String repetition ===".to_string());
    let mut pattern = std::sync::Arc::new(std::sync::Mutex::new(Some("Go! ".to_string())));
    let mut repeated = std::sync::Arc::new(std::sync::Mutex::new(Some((*strings.lock().unwrap().as_ref().unwrap()).repeat(std::sync::Arc::new(std::sync::Mutex::new(Some((*pattern.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some(3)))))));
    print!("Repeated: {}\n", (*repeated.lock().unwrap().as_ref().unwrap()));
    println!("{}", "\n=== String comparison ===".to_string());
    let mut str1 = std::sync::Arc::new(std::sync::Mutex::new(Some("apple".to_string())));
    let mut str2 = std::sync::Arc::new(std::sync::Mutex::new(Some("banana".to_string())));
    let mut str3 = std::sync::Arc::new(std::sync::Mutex::new(Some("apple".to_string())));
    print!("'{}' == '{}': {}\n", (*str1.lock().unwrap().as_ref().unwrap()), (*str2.lock().unwrap().as_ref().unwrap()), (*str1.lock().unwrap().as_ref().unwrap()) == (*str2.lock().unwrap().as_ref().unwrap()));
    print!("'{}' == '{}': {}\n", (*str1.lock().unwrap().as_ref().unwrap()), (*str3.lock().unwrap().as_ref().unwrap()), (*str1.lock().unwrap().as_ref().unwrap()) == (*str3.lock().unwrap().as_ref().unwrap()));
    print!("'{}' < '{}': {}\n", (*str1.lock().unwrap().as_ref().unwrap()), (*str2.lock().unwrap().as_ref().unwrap()), (*str1.lock().unwrap().as_ref().unwrap()) < (*str2.lock().unwrap().as_ref().unwrap()));
    print!("EqualFold('Apple', 'APPLE'): {}\n", (*strings.lock().unwrap().as_ref().unwrap()).equal_fold(std::sync::Arc::new(std::sync::Mutex::new(Some("Apple".to_string()))), std::sync::Arc::new(std::sync::Mutex::new(Some("APPLE".to_string())))));
    println!("{}", "\n=== String building ===".to_string());
    let mut builder;
    (*builder.lock().unwrap().as_ref().unwrap()).write_string(std::sync::Arc::new(std::sync::Mutex::new(Some("Building ".to_string()))));
    (*builder.lock().unwrap().as_ref().unwrap()).write_string(std::sync::Arc::new(std::sync::Mutex::new(Some("a ".to_string()))));
    (*builder.lock().unwrap().as_ref().unwrap()).write_string(std::sync::Arc::new(std::sync::Mutex::new(Some("string ".to_string()))));
    (*builder.lock().unwrap().as_ref().unwrap()).write_string(std::sync::Arc::new(std::sync::Mutex::new(Some("efficiently".to_string()))));
    let mut built = std::sync::Arc::new(std::sync::Mutex::new(Some((*builder.lock().unwrap().as_ref().unwrap()).string())));
    print!("Built string: {}\n", (*built.lock().unwrap().as_ref().unwrap()));
    print!("Builder length: {}\n", (*builder.lock().unwrap().as_ref().unwrap()).len());
    println!("{}", "\n=== Unicode and runes ===".to_string());
    let mut unicode = std::sync::Arc::new(std::sync::Mutex::new(Some("Hello, 世界! 🌍".to_string())));
    print!("Unicode string: {}\n", (*unicode.lock().unwrap().as_ref().unwrap()));
    print!("Byte length: {}\n", (*unicode.lock().unwrap().as_ref().unwrap()).len());
    let mut runeCount = std::sync::Arc::new(std::sync::Mutex::new(Some(0)));
    for (_, r) in (*unicode.lock().unwrap().as_ref().unwrap()).iter().enumerate() {
        { let mut guard = runeCount.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
        print!("Rune: %c (U+%04X)\n", r, r);
    }
    print!("Rune count: {}\n", (*runeCount.lock().unwrap().as_ref().unwrap()));
    println!("{}", "\n=== String trimming variations ===".to_string());
    let mut messy = std::sync::Arc::new(std::sync::Mutex::new(Some("!!!Hello World!!!".to_string())));
    print!("Original: {}\n", (*messy.lock().unwrap().as_ref().unwrap()));
    print!("TrimLeft '!': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).trim_left(std::sync::Arc::new(std::sync::Mutex::new(Some((*messy.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("!".to_string())))));
    print!("TrimRight '!': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).trim_right(std::sync::Arc::new(std::sync::Mutex::new(Some((*messy.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("!".to_string())))));
    print!("Trim '!': {}\n", (*strings.lock().unwrap().as_ref().unwrap()).trim(std::sync::Arc::new(std::sync::Mutex::new(Some((*messy.lock().unwrap().as_ref().unwrap())))), std::sync::Arc::new(std::sync::Mutex::new(Some("!".to_string())))));
    println!("{}", "\n=== String formatting ===".to_string());
    let mut name = std::sync::Arc::new(std::sync::Mutex::new(Some("Alice".to_string())));
    let mut age = std::sync::Arc::new(std::sync::Mutex::new(Some(30)));
    let mut height = std::sync::Arc::new(std::sync::Mutex::new(Some(5.6)));
    print!("Name: {}, Age: {}, Height: {:.1}\n", (*name.lock().unwrap().as_ref().unwrap()), (*age.lock().unwrap().as_ref().unwrap()), (*height.lock().unwrap().as_ref().unwrap()));
    print!("Quoted string: %q\n", (*name.lock().unwrap().as_ref().unwrap()));
    print!("String with width: '%10s'\n", (*name.lock().unwrap().as_ref().unwrap()));
    print!("Left-aligned: '%-10s'\n", (*name.lock().unwrap().as_ref().unwrap()));
}