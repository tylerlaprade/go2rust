use std::sync::{Arc, Mutex};

fn main() {
    let mut ticker = time.new_ticker(Arc::new(Mutex::new(Some(250 * (*(*time.lock().unwrap().as_mut().unwrap())::millisecond.lock().unwrap().as_ref().unwrap())))));
    let mut done = ;

    // TODO: Unhandled statement type: GoStmt

    time.sleep(Arc::new(Mutex::new(Some(800 * (*(*time.lock().unwrap().as_mut().unwrap())::millisecond.lock().unwrap().as_ref().unwrap())))));
    (*ticker.lock().unwrap().as_mut().unwrap()).stop();
    // TODO: Unhandled statement type: SendStmt
    println!("{}", "Ticker stopped".to_string());
}