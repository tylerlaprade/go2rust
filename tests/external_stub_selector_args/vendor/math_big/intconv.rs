use go2rust_stdlib_stubs::*;

use crate::{GoArrayElemMutRef, GoArrayElemPtr, GoArrayElemRef, GoPtr, GoSliceElemMutRef, GoSliceElemPtr, GoSliceElemRef, format_slice, format_slice_values, format_slice_wrapped, go_any_clone, go_strconv_format_float, go_strconv_format_int};

use crate::accuracy_string::*;
use crate::arith::*;
use crate::arith_decl::*;
use crate::decimal::*;
use crate::float::*;
use crate::floatconv::*;
use crate::floatmarsh::*;
use crate::ftoa::*;
use crate::int::*;
use crate::intmarsh::*;
use crate::nat::*;
use crate::natconv::*;
use crate::natdiv::*;
use crate::prime::*;
use crate::rat::*;
use crate::ratconv::*;
use crate::ratmarsh::*;
use crate::roundingmode_string::*;
use crate::sqrt::*;

use std::error::Error as StdError;
use std::fmt::{Display, Formatter};
use std::sync::{Arc, Mutex};

/// byteReader is a local wrapper around fmt.ScanState;
/// it implements the ByteReader interface.
#[derive(Clone, Default)]
pub struct byteReader {
    pub scan_state: Arc<Mutex<Option<fmt_ScanState>>>,
}

impl byteReader {
    pub fn __go_value_clone(&self) -> Self {
        Self { scan_state: self.scan_state.clone() }
    }
}

impl std::fmt::Display for byteReader {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{{{}}}", (*self.scan_state.lock().unwrap().as_ref().unwrap()))
    }
}

impl GoJsonDecode for byteReader {
    fn go_json_decode(value: &serde_json::Value) -> Result<Self, String> {
        let object = value.as_object().ok_or_else(|| go_json_expected(value, "object"))?;
        let mut out = Self::default();
        Ok(out)
    }
}


impl crate::int::Int {
    /// Text returns the string representation of x in the given base.
    /// Base must be between 2 and 62, inclusive. The result uses the
    /// lower-case letters 'a' to 'z' for digit values 10 to 35, and
    /// the upper-case letters 'A' to 'Z' for digit values 36 to 61.
    /// No prefix (such as "0x") is added to the string. If x is a nil
    /// pointer it returns "<nil>".
    pub fn text(&self, base: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<String>>> {
        if false {
        return Arc::new(Mutex::new(Some("<nil>".to_string())));
    }
        Arc::new(Mutex::new(Some(String::from_utf8((*(*self.abs.lock().unwrap().as_ref().unwrap()).itoa(Arc::new(Mutex::new(Some({ let __selector_holder = self.neg.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }))), Arc::new(Mutex::new(Some({ let __arg_holder = base.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() })))).lock().unwrap().as_ref().unwrap()).clone()).unwrap())))
    }

    /// Append appends the string representation of x, as generated by
    /// x.Text(base), to buf and returns the extended buffer.
    pub fn append(&self, buf: Arc<Mutex<Option<Vec<u8>>>>, base: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        if false {
        return { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("<nil>".to_string().as_bytes().iter().cloned()); __append_target.clone() };
    }
        { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = (*self.abs.lock().unwrap().as_ref().unwrap()).itoa(Arc::new(Mutex::new(Some({ let __selector_holder = self.neg.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }))), Arc::new(Mutex::new(Some({ let __arg_holder = base.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() })))).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }
    }

    /// String returns the decimal representation of x as generated by
    /// x.Text(10).
    pub fn string(&self) -> Arc<Mutex<Option<String>>> {
        self.text(Arc::new(Mutex::new(Some(10))))
    }

    /// Format implements [fmt.Formatter]. It accepts the formats
    /// 'b' (binary), 'o' (octal with 0 prefix), 'O' (octal with 0o prefix),
    /// 'd' (decimal), 'x' (lowercase hexadecimal), and
    /// 'X' (uppercase hexadecimal).
    /// Also supported are the full suite of package fmt's format
    /// flags for integral types, including '+' and ' ' for sign
    /// control, '#' for leading zero in octal and for hexadecimal,
    /// a leading "0x" or "0X" for "%#x" and "%#X" respectively,
    /// specification of minimum digits precision, output field
    /// width, space or zero padding, and '-' for left or right
    /// justification.
    pub fn format(&self, s: Arc<Mutex<Option<fmt_State>>>, ch: Arc<Mutex<Option<i32>>>) {
                // determine base
        let mut base: Arc<Mutex<Option<i32>>> = Arc::new(Mutex::new(Some(0)));
        { let _switch_val = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('b' as i32)) {
            { let new_val = 2; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('o' as i32)) || _switch_val == (('O' as i32)) {
            { let new_val = 8; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('d' as i32)) || _switch_val == (('s' as i32)) || _switch_val == (('v' as i32)) {
            { let new_val = 10; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('x' as i32)) || _switch_val == (('X' as i32)) {
            { let new_val = 16; *base.lock().unwrap() = Some(new_val); };
        } else {
                        // unknown format
            { let __s = format!("%!{}(big.Int={})", ({ let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v }) as u8 as char, (*self.string().lock().unwrap().as_ref().unwrap())); (*s.lock().unwrap().as_mut().unwrap()).write(Arc::new(Mutex::new(Some::<Vec<u8>>(__s.into_bytes())))) };
            return;
        }
    }
                // unknown format
        if false {
        fmt::fprint(s.clone(), ("<nil>".to_string(),));
        return;
    }
                // determine sign character
        let mut sign = Arc::new(Mutex::new(Some("".to_string())));
        if (*self.neg.clone().lock().unwrap().as_ref().unwrap()) {
            { let new_val = "-".to_string(); *sign.lock().unwrap() = Some(new_val); };
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag(('+' as i32)) {
            { let new_val = "+".to_string(); *sign.lock().unwrap() = Some(new_val); };
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag((' ' as i32)) {
            { let new_val = " ".to_string(); *sign.lock().unwrap() = Some(new_val); };
        }
                // supersedes ' ' when both specified
                // determine prefix characters for indicating output base
        let mut prefix = Arc::new(Mutex::new(Some("".to_string())));
        if (*s.lock().unwrap().as_ref().unwrap()).flag(('#' as i32)) {
        { let _switch_val = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('b' as i32)) {
            { let new_val = "0b".to_string(); *prefix.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('o' as i32)) {
            { let new_val = "0".to_string(); *prefix.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('x' as i32)) {
            { let new_val = "0x".to_string(); *prefix.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('X' as i32)) {
            { let new_val = "0X".to_string(); *prefix.lock().unwrap() = Some(new_val); };
        }
    }
    }
                // binary
                // octal
                // hexadecimal
        if { let __tmp_x = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = ('O' as i32); __tmp_x == __tmp_y } {
        { let new_val = "0o".to_string(); *prefix.lock().unwrap() = Some(new_val); };
    }
        let mut digits = (*self.abs.lock().unwrap().as_ref().unwrap()).utoa(Arc::new(Mutex::new(Some({ let __arg_holder = base.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        if { let __tmp_x = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = ('X' as i32); __tmp_x == __tmp_y } {
                // faster than bytes.ToUpper
        { let __range_holder = digits.clone(); let __range_guard = __range_holder.lock().unwrap(); let __range_values = __range_guard.as_ref().cloned().unwrap_or_default(); drop(__range_guard); for (i, d) in __range_values.iter().copied().enumerate() {
        if { let __tmp_x = ('a' as i32) as u8; let __tmp_y = d; __tmp_x <= __tmp_y } && { let __tmp_x = d; let __tmp_y = ('z' as i32) as u8; __tmp_x <= __tmp_y } {
        (*digits.lock().unwrap().as_mut().unwrap())[(i) as usize] = { let __tmp_x = ('A' as i32) as u8; let __tmp_y = ({ let __tmp_x = d; let __tmp_y = ('a' as i32) as u8; __tmp_x - __tmp_y }); __tmp_x + __tmp_y };
    }
    } }
    }
                // faster than bytes.ToUpper
                // number of characters for the three classes of number padding
        let mut left: Arc<Mutex<Option<i32>>> = Arc::new(Mutex::new(Some(0)));
        let mut zeros: Arc<Mutex<Option<i32>>> = Arc::new(Mutex::new(Some(0)));
        let mut right: Arc<Mutex<Option<i32>>> = Arc::new(Mutex::new(Some(0)));
                // determine number padding from precision: the least number of digits to output
        let (mut precision, mut precisionSet) = (*s.lock().unwrap().as_ref().unwrap()).precision();
        if precisionSet {
        if { let __tmp_x = ({ let __v = (*digits.lock().unwrap().as_ref().unwrap()).clone(); __v }.len() as i32); let __tmp_y = (precision as i32); __tmp_x < __tmp_y } {
            { let new_val = { let __tmp_x = (precision as i32); let __tmp_y = ({ let __v = (*digits.lock().unwrap().as_ref().unwrap()).clone(); __v }.len() as i32); __tmp_x - __tmp_y }; *zeros.lock().unwrap() = Some(new_val); };
        } else if { let __tmp_x = ({ let __v = (*digits.lock().unwrap().as_ref().unwrap()).clone(); __v }.len() as i32); let __tmp_y = 1; __tmp_x == __tmp_y } && { let __tmp_x = { let __seq = { let __seq_holder = digits.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(0) as usize].clone() }; let __tmp_y = ('0' as i32) as u8; __tmp_x == __tmp_y } && { let __tmp_x = precision; let __tmp_y = 0; __tmp_x == __tmp_y } {
            return;
        }
    }
                // count of zero padding
                // print nothing if zero value (x == 0) and zero precision ("." or ".0")
                // determine field pad from width: the least number of characters to output
        let mut length = Arc::new(Mutex::new(Some({ let __tmp_x = ({ let __tmp_x = ({ let __tmp_x = ((*sign.lock().unwrap().as_ref().unwrap()).len() as i32); let __tmp_y = ((*prefix.lock().unwrap().as_ref().unwrap()).len() as i32); __tmp_x + __tmp_y } as i32); let __tmp_y = ({ let __v = (*zeros.lock().unwrap().as_ref().unwrap()).clone(); __v } as i32); __tmp_x + __tmp_y } as i32); let __tmp_y = ({ let __v = (*digits.lock().unwrap().as_ref().unwrap()).clone(); __v }.len() as i32); __tmp_x + __tmp_y })));
        {
        let (mut width, mut widthSet) = (*s.lock().unwrap().as_ref().unwrap()).width();;
        if widthSet && { let __tmp_x = { let __v = (*length.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = width; __tmp_x < __tmp_y } {
            let mut d = Arc::new(Mutex::new(Some({ let __tmp_x = width; let __tmp_y = { let __v = (*length.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x - __tmp_y })));
    if (*s.lock().unwrap().as_ref().unwrap()).flag(('-' as i32)) {
            { let new_val = d.lock().unwrap().as_ref().unwrap().clone(); *right.lock().unwrap() = Some(new_val); };
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag(('0' as i32)) && !precisionSet {
            { let new_val = d.lock().unwrap().as_ref().unwrap().clone(); *zeros.lock().unwrap() = Some(new_val); };
        } else {
            { let new_val = d.lock().unwrap().as_ref().unwrap().clone(); *left.lock().unwrap() = Some(new_val); };
        };
        }
    }
                // pad on the right with spaces; supersedes '0' when both specified
                // pad with zeros unless precision also specified
                // pad on the left with spaces
                // print number as [left pad][sign][prefix][zero pad][digits][right pad]
        write_multiple(s.clone(), Arc::new(Mutex::new(Some(" ".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = left.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        write_multiple(s.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = sign.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(1))));
        write_multiple(s.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = prefix.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(1))));
        write_multiple(s.clone(), Arc::new(Mutex::new(Some("0".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = zeros.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        (*s.lock().unwrap().as_ref().unwrap()).write(digits.clone());
        write_multiple(s.clone(), Arc::new(Mutex::new(Some(" ".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = right.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
    }

    /// scan sets z to the integer value corresponding to the longest possible prefix
    /// read from r representing a signed integer number in a given conversion base.
    /// It returns z, the actual conversion base used, and an error, if any. In the
    /// error case, the value of z is undefined but the returned value is nil. The
    /// syntax follows the syntax of integer literals in Go.
    ///
    /// The base argument must be 0 or a value from 2 through MaxBase. If the base
    /// is 0, the string prefix determines the actual conversion base. A prefix of
    /// “0b” or “0B” selects base 2; a “0”, “0o”, or “0O” prefix selects
    /// base 8, and a “0x” or “0X” prefix selects base 16. Otherwise the selected
    /// base is 10.
    pub fn scan_1(&mut self, r: Arc<Mutex<Option<io_ByteScanner>>>, mut base: Arc<Mutex<Option<i32>>>) -> (Arc<Mutex<Option<crate::int::Int>>>, i32, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
                // determine sign
        let (mut neg, mut err) = scan_sign(r.clone());
        if { let __nil_result = (*err.lock().unwrap()).is_some(); __nil_result } {
        return (Arc::new(Mutex::new(None)), 0, err.clone());
    }
                // determine mantissa
        { let (__tmp_0, __tmp_1, __tmp_2, __tmp_3) = (*self.abs.lock().unwrap().as_ref().unwrap()).scan(r.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = base.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(false)))); let __moved_tmp_0 = { let mut __guard = __tmp_0.lock().unwrap(); __guard.take() }; *self.abs.lock().unwrap() = __moved_tmp_0; *base.lock().unwrap() = Some(__tmp_1); let __moved_tmp_3 = { let mut __guard = __tmp_3.lock().unwrap(); __guard.take() }; *err.lock().unwrap() = __moved_tmp_3; };
        if { let __nil_result = (*err.lock().unwrap()).is_some(); __nil_result } {
        return (Arc::new(Mutex::new(None)), { let __v = (*base.lock().unwrap().as_ref().unwrap()).clone(); __v }, err.clone());
    }
        { let new_val = { let __tmp_x = ({ let __slice_holder = { let __named_slice = (*self.abs.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) } as i32); let __tmp_y = 0; __tmp_x > __tmp_y } && neg; *self.neg.lock().unwrap() = Some(new_val); };
        (Arc::new(Mutex::new(Some(self.clone()))), { let __v = (*base.lock().unwrap().as_ref().unwrap()).clone(); __v }, Arc::new(Mutex::new(None)))
    }

    /// Scan is a support routine for [fmt.Scanner]; it sets z to the value of
    /// the scanned number. It accepts the formats 'b' (binary), 'o' (octal),
    /// 'd' (decimal), 'x' (lowercase hexadecimal), and 'X' (uppercase hexadecimal).
    pub fn scan(&mut self, s: Arc<Mutex<Option<fmt_ScanState>>>, ch: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>> {
        (*s.lock().unwrap().as_ref().unwrap()).skip_space();
        let mut base = Arc::new(Mutex::new(Some(0)));
        { let _switch_val = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('b' as i32)) {
            { let new_val = 2; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('o' as i32)) {
            { let new_val = 8; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('d' as i32)) {
            { let new_val = 10; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('x' as i32)) || _switch_val == (('X' as i32)) {
            { let new_val = 16; *base.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('s' as i32)) || _switch_val == (('v' as i32)) {
        } else {
            return Arc::new(Mutex::new(Some(Box::<dyn std::error::Error + Send + Sync>::from("Int.Scan: invalid verb".to_string()))));
        }
    }
                // let scan determine the base
        let (_, _, mut err) = self.scan_1(Arc::new(Mutex::new(Some(io_ByteScanner::__go_from(byteReader { scan_state: s.clone(), ..Default::default() })))), Arc::new(Mutex::new(Some({ let __arg_holder = base.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        return err.clone();
    }
}

impl byteReader {
    pub fn read_byte(&self) -> (u8, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
        let (mut ch, mut size, mut err) = { let __promoted_recv = self.scan_state.clone(); let __promoted_guard = __promoted_recv.lock().unwrap(); let __promoted_ref = __promoted_guard.as_ref().unwrap(); let __result = __promoted_ref.read_rune(); __result };
        if { let __tmp_x = size; let __tmp_y = 1; __tmp_x != __tmp_y } && { let __nil_result = (*err.lock().unwrap()).is_none(); __nil_result } {
        { let __rhs_holder = Arc::new(Mutex::new(Some(Box::<dyn StdError + Send + Sync>::from(format!("invalid rune U+{:04X}", ch as u32))))).clone(); let new_val = { let mut guard = __rhs_holder.lock().unwrap(); guard.take() }; *err.lock().unwrap() = new_val; };
    }
        return ((*Arc::new(Mutex::new(Some(ch as u8))).lock().unwrap().as_ref().unwrap()), err.clone());
    }

    pub fn unread_byte(&self) -> Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>> {
        { let __promoted_recv = self.scan_state.clone(); let __promoted_guard = __promoted_recv.lock().unwrap(); let __promoted_ref = __promoted_guard.as_ref().unwrap(); let __result = __promoted_ref.unread_rune(); __result }
    }

    pub fn read<T0: 'static>(&self, _arg0: T0) -> (i32, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.read(_arg0)
    }

    pub fn read_rune(&self) -> (i32, i32, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.read_rune()
    }

    pub fn skip_space(&self) {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.skip_space()
    }

    pub fn token<T0: 'static, T1: 'static>(&self, _arg0: T0, _arg1: T1) -> (Arc<Mutex<Option<Vec<u8>>>>, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.token(_arg0, _arg1)
    }

    pub fn unread_rune(&self) -> Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>> {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.unread_rune()
    }

    pub fn width(&self) -> (i32, bool) {
        let embedded = self.scan_state.clone();
        let guard = embedded.lock().unwrap();
        let embedded_ref = guard.as_ref().unwrap();
        embedded_ref.width()
    }
}

/// write count copies of text to s.
pub fn write_multiple(s: Arc<Mutex<Option<fmt_State>>>, text: Arc<Mutex<Option<String>>>, mut count: Arc<Mutex<Option<i32>>>) {
    if { let __tmp_x = ((*text.lock().unwrap().as_ref().unwrap()).len() as i32); let __tmp_y = 0; __tmp_x > __tmp_y } {
        let mut b = Arc::new(Mutex::new(Some(({ let __v = (*text.lock().unwrap().as_ref().unwrap()).clone(); __v }).as_bytes().to_vec())));
        while { let __tmp_x = { let __v = (*count.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
        (*s.lock().unwrap().as_ref().unwrap()).write(b.clone());
        { let mut guard = count.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() - 1); }
    }
    }
}

pub fn scan_sign(r: Arc<Mutex<Option<io_ByteScanner>>>) -> (bool, Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>>) {
    let mut neg: Arc<Mutex<Option<bool>>> = Arc::new(Mutex::new(Some(false)));
    let mut err: Arc<Mutex<Option<Box<dyn StdError + Send + Sync>>>> = Arc::new(Mutex::new(None));

    let mut ch: Arc<Mutex<Option<u8>>> = Arc::new(Mutex::new(Some(0)));
    {
        { let (__tmp_0, __tmp_1) = (*r.lock().unwrap().as_ref().unwrap()).read_byte(); *ch.lock().unwrap() = Some(__tmp_0); let __moved_tmp_1 = { let mut __guard = __tmp_1.lock().unwrap(); __guard.take() }; *err.lock().unwrap() = __moved_tmp_1; };;
        if { let __nil_result = (*err.lock().unwrap()).is_some(); __nil_result } {
            return (false, err.clone());;
        }
    }
    { let _switch_val = { let __v = (*ch.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('-' as i32) as u8) {
            { let new_val = true; *neg.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('+' as i32) as u8) {
        } else {
            (*r.lock().unwrap().as_ref().unwrap()).unread_byte();
        }
    }
        // nothing to do
    return ((*neg.lock().unwrap().as_ref().unwrap()), err.clone());
}

pub(crate) fn __go_init_functions() {
}


pub(crate) fn __go_init_all() {
}


impl GoValueClone for byteReader {
    fn go_value_clone(&self) -> Self {
        self.__go_value_clone()
    }
}
