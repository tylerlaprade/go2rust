use go2rust_stdlib_stubs::*;

use crate::{GoArrayElemMutRef, GoArrayElemPtr, GoArrayElemRef, GoMutex, GoOnce, GoPtr, GoSliceElemMutRef, GoSliceElemPtr, GoSliceElemRef, format_slice, format_slice_values, format_slice_wrapped, go_any_clone, go_strconv_format_float, go_strconv_format_int};

use crate::accuracy_string::*;
use crate::arith::*;
use crate::arith_decl::*;
use crate::decimal::*;
use crate::float::*;
use crate::floatconv::*;
use crate::floatmarsh::*;
use crate::int::*;
use crate::intconv::*;
use crate::intmarsh::*;
use crate::nat::*;
use crate::natconv::*;
use crate::natdiv::*;
use crate::prime::*;
use crate::rat::*;
use crate::ratconv::*;
use crate::ratmarsh::*;
use crate::roundingmode_string::*;
use crate::sqrt::*;

use std::any::Any;
use std::error::Error as StdError;
use std::sync::{Arc, Mutex};

impl crate::float::Float {
    /// Text converts the floating-point number x to a string according
    /// to the given format and precision prec. The format is one of:
    ///
    ///	'e'	-d.dddde±dd, decimal exponent, at least two (possibly 0) exponent digits
    ///	'E'	-d.ddddE±dd, decimal exponent, at least two (possibly 0) exponent digits
    ///	'f'	-ddddd.dddd, no exponent
    ///	'g'	like 'e' for large exponents, like 'f' otherwise
    ///	'G'	like 'E' for large exponents, like 'f' otherwise
    ///	'x'	-0xd.dddddp±dd, hexadecimal mantissa, decimal power of two exponent
    ///	'p'	-0x.dddp±dd, hexadecimal mantissa, decimal power of two exponent (non-standard)
    ///	'b'	-ddddddp±dd, decimal mantissa, decimal power of two exponent (non-standard)
    ///
    /// For the power-of-two exponent formats, the mantissa is printed in normalized form:
    ///
    ///	'x'	hexadecimal mantissa in [1, 2), or 0
    ///	'p'	hexadecimal mantissa in [½, 1), or 0
    ///	'b'	decimal integer mantissa using x.Prec() bits, or 0
    ///
    /// Note that the 'x' form is the one used by most other languages and libraries.
    ///
    /// If format is a different character, Text returns a "%" followed by the
    /// unrecognized format character.
    ///
    /// The precision prec controls the number of digits (excluding the exponent)
    /// printed by the 'e', 'E', 'f', 'g', 'G', and 'x' formats.
    /// For 'e', 'E', 'f', and 'x', it is the number of digits after the decimal point.
    /// For 'g' and 'G' it is the total number of digits. A negative precision selects
    /// the smallest number of decimal digits necessary to identify the value x uniquely
    /// using x.Prec() mantissa bits.
    /// The prec value is ignored for the 'b' and 'p' formats.
    pub fn text(&mut self, format: Arc<Mutex<Option<u8>>>, prec: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<String>>> {
        let mut cap = Arc::new(Mutex::new(Some(10)));
        if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let __rhs = (*prec.lock().unwrap().as_ref().unwrap()); let mut guard = cap.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + __rhs); };
    }
        return Arc::new(Mutex::new(Some(String::from_utf8((*self.append(Arc::new(Mutex::new(Some(Vec::<u8>::with_capacity(({ let __v = (*cap.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize)))), Arc::new(Mutex::new(Some({ let __arg_holder = format.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some({ let __arg_holder = prec.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() })))).lock().unwrap().as_ref().unwrap()).clone()).unwrap())));
    }

    /// String formats x like x.Text('g', 10).
    /// (String must be called explicitly, [Float.Format] does not support %s verb.)
    pub fn string(&mut self) -> Arc<Mutex<Option<String>>> {
        self.text(Arc::new(Mutex::new(Some(('g' as i32) as u8))), Arc::new(Mutex::new(Some(10))))
    }

    /// Append appends to buf the string form of the floating-point number x,
    /// as generated by x.Text, and returns the extended buffer.
    pub fn append(&mut self, mut buf: Arc<Mutex<Option<Vec<u8>>>>, fmt: Arc<Mutex<Option<u8>>>, mut prec: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
                // sign
        if (*self.neg.clone().lock().unwrap().as_ref().unwrap()) {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('-' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
                // Inf
        if { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(INF as u8)))); __tmp_x == __tmp_y } {
        if !(*self.neg.clone().lock().unwrap().as_ref().unwrap()) {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('+' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
        return { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("Inf".to_string().as_bytes().iter().cloned()); __append_target.clone() };
    }
                // pick off easy formats
        { let _switch_val = { let __v = (*fmt.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('b' as i32) as u8) {
            return self.fmt_b(buf.clone());
        } else if _switch_val == (('p' as i32) as u8) {
            return self.fmt_p(buf.clone());
        } else if _switch_val == (('x' as i32) as u8) {
            return self.fmt_x(buf.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = prec.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        }
    }
                // Algorithm:
                //   1) convert Float to multiprecision decimal
                //   2) round to desired precision
                //   3) read digits out and format
                // 1) convert Float to multiprecision decimal
        let mut d: Arc<Mutex<Option<decimal>>> = Arc::new(Mutex::new(Some(Default::default())));
        if { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(FINITE as u8)))); __tmp_x == __tmp_y } {
                // x != 0
        (*d.lock().unwrap().as_mut().unwrap()).init({ let __field = self.mant.clone(); __field }, Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __selector_holder = self.exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i32))).lock().unwrap().as_ref().unwrap()); let __tmp_y = (*self.mant.lock().unwrap().as_ref().unwrap()).bit_len(); __tmp_x - __tmp_y }))));
    }
                // x != 0
                // 2) round to desired precision
        let mut shortest = Arc::new(Mutex::new(Some(false)));
        if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x < __tmp_y } {
        { let new_val = true; *shortest.lock().unwrap() = Some(new_val); };
        round_shortest(d.clone(), Arc::new(Mutex::new(Some(self.clone()))));
                // Precision for shortest representation mode.
        { let _switch_val = { let __v = (*fmt.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('e' as i32) as u8) || _switch_val == (('E' as i32) as u8) {
            { let new_val = { let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = 1; __tmp_x - __tmp_y }; *prec.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('f' as i32) as u8) {
            { let new_val = std::cmp::max(({ let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = ((*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()) as i32); __tmp_x - __tmp_y } as i32), (0 as i32)); *prec.lock().unwrap() = Some(new_val); };
        } else if _switch_val == (('g' as i32) as u8) || _switch_val == (('G' as i32) as u8) {
            { let new_val = ({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32; *prec.lock().unwrap() = Some(new_val); };
        }
    }
    } else {
                // round appropriately
        { let _switch_val = { let __v = (*fmt.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('e' as i32) as u8) || _switch_val == (('E' as i32) as u8) {
                        // one digit before and number of digits after decimal point
            (*d.lock().unwrap().as_mut().unwrap()).round(Arc::new(Mutex::new(Some({ let __tmp_x = 1; let __tmp_y = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x + __tmp_y }))));
        } else if _switch_val == (('f' as i32) as u8) {
                        // number of digits before and after decimal point
            (*d.lock().unwrap().as_mut().unwrap()).round(Arc::new(Mutex::new(Some({ let __tmp_x = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); let __tmp_y = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x + __tmp_y }))));
        } else if _switch_val == (('g' as i32) as u8) || _switch_val == (('G' as i32) as u8) {
            if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x == __tmp_y } {
        { let new_val = 1; *prec.lock().unwrap() = Some(new_val); };
    }
            (*d.lock().unwrap().as_mut().unwrap()).round(Arc::new(Mutex::new(Some({ let __arg_holder = prec.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        }
    }
    }
                // Precision for shortest representation mode.
                // round appropriately
                // one digit before and number of digits after decimal point
                // number of digits before and after decimal point
                // 3) read digits out and format
        { let _switch_val = { let __v = (*fmt.lock().unwrap().as_ref().unwrap()).clone(); __v };
    if _switch_val == (('e' as i32) as u8) || _switch_val == (('E' as i32) as u8) {
            return fmt_e(buf.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = fmt.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some({ let __arg_holder = prec.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some({ let __arg_holder = d.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        } else if _switch_val == (('f' as i32) as u8) {
            return fmt_f(buf.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = prec.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some({ let __arg_holder = d.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        } else if _switch_val == (('g' as i32) as u8) || _switch_val == (('G' as i32) as u8) {
                        // trim trailing fractional zeros in %e format
            let mut eprec = { let __owned = prec.lock().unwrap().as_ref().unwrap().clone(); Arc::new(Mutex::new(Some(__owned))) };
            if { let __tmp_x = ({ let __v = (*eprec.lock().unwrap().as_ref().unwrap()).clone(); __v } as i32); let __tmp_y = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); __tmp_x > __tmp_y } && { let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = ((*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()) as i32); __tmp_x >= __tmp_y } {
        { let new_val = ({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32; *eprec.lock().unwrap() = Some(new_val); };
    }
                        // %e is used if the exponent from the conversion
                        // is less than -4 or greater than or equal to the precision.
                        // If precision was the shortest possible, use eprec = 6 for
                        // this decision.
            if { let __v = (*shortest.lock().unwrap().as_ref().unwrap()).clone(); __v } {
        { let new_val = 6; *eprec.lock().unwrap() = Some(new_val); };
    }
            let mut exp = Arc::new(Mutex::new(Some({ let __tmp_x = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); let __tmp_y = 1; __tmp_x - __tmp_y })));
            if { let __tmp_x = { let __v = (*exp.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = -4; __tmp_x < __tmp_y } || { let __tmp_x = { let __v = (*exp.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*eprec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x >= __tmp_y } {
        if { let __tmp_x = ({ let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v } as i32); let __tmp_y = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); __tmp_x > __tmp_y } {
        { let new_val = ({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32; *prec.lock().unwrap() = Some(new_val); };
    }
        return fmt_e(buf.clone(), Arc::new(Mutex::new(Some({ let __tmp_x = { let __tmp_x = { let __v = (*fmt.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = ('e' as i32) as u8; __tmp_x + __tmp_y }; let __tmp_y = ('g' as i32) as u8; __tmp_x - __tmp_y }))), Arc::new(Mutex::new(Some({ let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 1; __tmp_x - __tmp_y }))), Arc::new(Mutex::new(Some({ let __arg_holder = d.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
    }
            if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); __tmp_x > __tmp_y } {
        { let new_val = ({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32; *prec.lock().unwrap() = Some(new_val); };
    }
            return fmt_f(buf.clone(), Arc::new(Mutex::new(Some(std::cmp::max(({ let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); __tmp_x - __tmp_y } as i32), (0 as i32))))), Arc::new(Mutex::new(Some({ let __arg_holder = d.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        }
    }
                // trim trailing fractional zeros in %e format
                // %e is used if the exponent from the conversion
                // is less than -4 or greater than or equal to the precision.
                // If precision was the shortest possible, use eprec = 6 for
                // this decision.
                // unknown format
        if (*self.neg.clone().lock().unwrap().as_ref().unwrap()) {
        { let new_val = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = buf.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; let __high = ({ let __tmp_x = ((*buf.lock().unwrap()).as_ref().map(|__v| __v.len()).unwrap_or(0) as i32); let __tmp_y = 1; __tmp_x - __tmp_y }) as usize; __seq[..__high].to_vec() }))); buf = new_val; };
    }
                // sign was added prematurely - remove it again
        { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend(vec![('%' as i32) as u8, (*fmt.lock().unwrap().as_ref().unwrap()).clone()]); __append_target.clone() }
    }

    /// fmtB appends the string of x in the format mantissa "p" exponent
    /// with a decimal mantissa and a binary exponent, or "0" if x is zero,
    /// and returns the extended buffer.
    /// The mantissa is normalized such that is uses x.Prec() bits in binary
    /// representation.
    /// The sign of x is ignored, and x must not be an Inf.
    /// (The caller handles Inf before invoking fmtB.)
    pub fn fmt_b(&self, mut buf: Arc<Mutex<Option<Vec<u8>>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        if { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(ZERO as u8)))); __tmp_x == __tmp_y } {
        return { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() };
    }
        if DEBUG_FLOAT && { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(FINITE as u8)))); __tmp_x != __tmp_y } {
        std::panic::panic_any(Box::new("non-finite float".to_string()) as Box<dyn Any + Send + Sync>);
    }
                // x != 0
                // adjust mantissa to use exactly x.prec bits
        let mut m = Arc::new(Mutex::new(Some({ let __selector_holder = self.mant.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned })));
        let mut w = Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __slice_holder = { let __named_slice = (*self.mant.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) } as u32))).lock().unwrap().as_ref().unwrap()); let __tmp_y = __W as u32; __tmp_x * __tmp_y })));
    if { let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*self.prec.lock().unwrap().as_ref().unwrap()); __tmp_x < __tmp_y } {
            { let new_val = crate::nat::nat(Arc::new(Mutex::new(None::<Vec<crate::arith::Word>>))).shl(m.clone(), Arc::new(Mutex::new(Some(({ let __tmp_x = (*self.prec.lock().unwrap().as_ref().unwrap()); let __tmp_y = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x - __tmp_y }) as u64)))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *m.lock().unwrap() = __moved_val; };
        } else if { let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*self.prec.lock().unwrap().as_ref().unwrap()); __tmp_x > __tmp_y } {
            { let new_val = crate::nat::nat(Arc::new(Mutex::new(None::<Vec<crate::arith::Word>>))).shr(m.clone(), Arc::new(Mutex::new(Some(({ let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*self.prec.lock().unwrap().as_ref().unwrap()); __tmp_x - __tmp_y }) as u64)))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *m.lock().unwrap() = __moved_val; };
        }
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = (*m.lock().unwrap().as_ref().unwrap()).utoa(Arc::new(Mutex::new(Some(10)))).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }; buf = new_val; };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('p' as i32) as u8); __append_target.clone() }; buf = new_val; };
        let mut e = Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __selector_holder = self.exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i64))).lock().unwrap().as_ref().unwrap()); let __tmp_y = (*Arc::new(Mutex::new(Some({ let __selector_holder = self.prec.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i64))).lock().unwrap().as_ref().unwrap()); __tmp_x - __tmp_y })));
        if { let __tmp_x = { let __v = (*e.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0 as i64; __tmp_x >= __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('+' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
        return strconv::append_int(buf.clone(), { let __arg_holder = e.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }, 10);
    }

    /// fmtX appends the string of x in the format "0x1." mantissa "p" exponent
    /// with a hexadecimal mantissa and a binary exponent, or "0x0p0" if x is zero,
    /// and returns the extended buffer.
    /// A non-zero mantissa is normalized such that 1.0 <= mantissa < 2.0.
    /// The sign of x is ignored, and x must not be an Inf.
    /// (The caller handles Inf before invoking fmtX.)
    pub fn fmt_x(&mut self, mut buf: Arc<Mutex<Option<Vec<u8>>>>, prec: Arc<Mutex<Option<i32>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        let mut __self = self.clone();
        if { let __tmp_x = { let __selector_holder = __self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(ZERO as u8)))); __tmp_x == __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("0x0".to_string().as_bytes().iter().cloned()); __append_target.clone() }; buf = new_val; };
        if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('.' as i32) as u8); __append_target.clone() }; buf = new_val; };
        let mut i = Arc::new(Mutex::new(Some(0)));
    while { let __tmp_x = { let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
        { let mut guard = i.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
    }
    }
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("p+00".to_string().as_bytes().iter().cloned()); __append_target.clone() }; buf = new_val; };
        return buf.clone();
    }
        if DEBUG_FLOAT && { let __tmp_x = { let __selector_holder = __self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(FINITE as u8)))); __tmp_x != __tmp_y } {
        std::panic::panic_any(Box::new("non-finite float".to_string()) as Box<dyn Any + Send + Sync>);
    }
                // round mantissa to n bits
        let mut n: Arc<Mutex<Option<u64>>> = Arc::new(Mutex::new(Some(0)));
        if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x < __tmp_y } {
        { let new_val = { let __tmp_x = 1 as u64; let __tmp_y = { let __tmp_x = { let __tmp_x = ({ let __tmp_x = { let __tmp_x = __self.min_prec(); let __tmp_y = 1 as u64; __tmp_x - __tmp_y }; let __tmp_y = 3 as u64; __tmp_x + __tmp_y }); let __tmp_y = 4 as u64; __tmp_x / __tmp_y }; let __tmp_y = 4 as u64; __tmp_x * __tmp_y }; __tmp_x + __tmp_y }; *n.lock().unwrap() = Some(new_val); };
    } else {
        { let new_val = { let __tmp_x = 1 as u64; let __tmp_y = { let __tmp_x = 4 as u64; let __tmp_y = (*Arc::new(Mutex::new(Some((*prec.lock().unwrap().as_ref().unwrap()) as u64))).lock().unwrap().as_ref().unwrap()); __tmp_x * __tmp_y }; __tmp_x + __tmp_y }; *n.lock().unwrap() = Some(new_val); };
    }
                // round MinPrec up to 1 mod 4
                // n%4 == 1
        { let new_val = { let __recv = { let __recv = { let __recv = Arc::new(Mutex::new(Some(Float::default()))); let __result = (*__recv.lock().unwrap().as_mut().unwrap()).set_prec(Arc::new(Mutex::new(Some({ let __arg_holder = n.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() })))); __result }; let __result = (*__recv.lock().unwrap().as_mut().unwrap()).set_mode(Arc::new(Mutex::new(Some({ let __selector_holder = __self.mode.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned })))); __result }; let __result = (*__recv.lock().unwrap().as_mut().unwrap()).set(Arc::new(Mutex::new(Some(__self.clone())))); __result }; let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take().unwrap() }; __self = __moved_val; };
                // adjust mantissa to use exactly n bits
        let mut m = Arc::new(Mutex::new(Some({ let __selector_holder = __self.mant.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned })));
        let mut w = Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __slice_holder = { let __named_slice = (*__self.mant.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) } as u64))).lock().unwrap().as_ref().unwrap()); let __tmp_y = __W as u64; __tmp_x * __tmp_y })));
    if { let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*n.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x < __tmp_y } {
            { let new_val = crate::nat::nat(Arc::new(Mutex::new(None::<Vec<crate::arith::Word>>))).shl(m.clone(), Arc::new(Mutex::new(Some({ let __tmp_x = { let __v = (*n.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x - __tmp_y })))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *m.lock().unwrap() = __moved_val; };
        } else if { let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*n.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x > __tmp_y } {
            { let new_val = crate::nat::nat(Arc::new(Mutex::new(None::<Vec<crate::arith::Word>>))).shr(m.clone(), Arc::new(Mutex::new(Some({ let __tmp_x = { let __v = (*w.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*n.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x - __tmp_y })))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *m.lock().unwrap() = __moved_val; };
        }
        let mut exp64 = Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __selector_holder = __self.exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i64))).lock().unwrap().as_ref().unwrap()); let __tmp_y = 1 as i64; __tmp_x - __tmp_y })));
        let mut hm = (*m.lock().unwrap().as_ref().unwrap()).utoa(Arc::new(Mutex::new(Some(16))));
        if DEBUG_FLOAT && { let __tmp_x = { let __seq = { let __seq_holder = hm.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(0) as usize].clone() }; let __tmp_y = ('1' as i32) as u8; __tmp_x != __tmp_y } {
        std::panic::panic_any(Box::new(format!("{}{}", "incorrect mantissa: ".to_string(), (*Arc::new(Mutex::new(Some(String::from_utf8((*hm.lock().unwrap().as_ref().unwrap()).clone()).unwrap()))).lock().unwrap().as_ref().unwrap()))) as Box<dyn Any + Send + Sync>);
    }
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("0x1".to_string().as_bytes().iter().cloned()); __append_target.clone() }; buf = new_val; };
        if { let __tmp_x = ((*hm.lock().unwrap()).as_ref().map(|__v| __v.len()).unwrap_or(0) as i32); let __tmp_y = 1; __tmp_x > __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('.' as i32) as u8); __append_target.clone() }; buf = new_val; };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = hm.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(1) as usize..].to_vec() }))).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }; buf = new_val; };
    }
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('p' as i32) as u8); __append_target.clone() }; buf = new_val; };
        if { let __tmp_x = { let __v = (*exp64.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0 as i64; __tmp_x >= __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('+' as i32) as u8); __append_target.clone() }; buf = new_val; };
    } else {
        { let new_val = -((*exp64.lock().unwrap().as_ref().unwrap())); *exp64.lock().unwrap() = Some(new_val); };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('-' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
                // Force at least two exponent digits, to match fmt.
        if { let __tmp_x = { let __v = (*exp64.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 10 as i64; __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
        return strconv::append_int(buf.clone(), { let __arg_holder = exp64.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }, 10);
    }

    /// fmtP appends the string of x in the format "0x." mantissa "p" exponent
    /// with a hexadecimal mantissa and a binary exponent, or "0" if x is zero,
    /// and returns the extended buffer.
    /// The mantissa is normalized such that 0.5 <= 0.mantissa < 1.0.
    /// The sign of x is ignored, and x must not be an Inf.
    /// (The caller handles Inf before invoking fmtP.)
    pub fn fmt_p(&self, mut buf: Arc<Mutex<Option<Vec<u8>>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        if { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(ZERO as u8)))); __tmp_x == __tmp_y } {
        return { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() };
    }
        if DEBUG_FLOAT && { let __tmp_x = { let __selector_holder = self.form.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned }; let __tmp_y = crate::float::form(Arc::new(Mutex::new(Some(FINITE as u8)))); __tmp_x != __tmp_y } {
        std::panic::panic_any(Box::new("non-finite float".to_string()) as Box<dyn Any + Send + Sync>);
    }
                // x != 0
                // remove trailing 0 words early
                // (no need to convert to hex 0's and trim later)
        let mut m = Arc::new(Mutex::new(Some({ let __selector_holder = self.mant.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned })));
        let mut i = Arc::new(Mutex::new(Some(0)));
        while { let __tmp_x = ({ let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v } as i32); let __tmp_y = ({ let __slice_holder = { let __named_slice = (*m.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) } as i32); __tmp_x < __tmp_y } && { let __tmp_x = { let __seq_holder = { let __named_slice = (*m.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __seq_guard = __seq_holder.lock().unwrap(); let __seq = __seq_guard.as_ref().unwrap(); __seq[({ let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize].clone() }; let __tmp_y = crate::arith::Word(Arc::new(Mutex::new(Some(0 as u64)))); __tmp_x == __tmp_y } {
        { let mut guard = i.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
    }
        { let new_val = crate::nat::nat(Arc::new(Mutex::new(Some({ let __slice_holder = { let __named_slice = (*m.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __slice_guard = __slice_holder.lock().unwrap(); let __seq = __slice_guard.as_ref().cloned().unwrap_or_default(); __seq[({ let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize..].to_vec() })))); *m.lock().unwrap() = Some(new_val); };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend("0x.".to_string().as_bytes().iter().cloned()); __append_target.clone() }; buf = new_val; };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = bytes::trim_right((*m.lock().unwrap().as_ref().unwrap()).utoa(Arc::new(Mutex::new(Some(16)))), "0".to_string()).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }; buf = new_val; };
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('p' as i32) as u8); __append_target.clone() }; buf = new_val; };
        if { let __tmp_x = (*self.exp.lock().unwrap().as_ref().unwrap()); let __tmp_y = 0 as i32; __tmp_x >= __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('+' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
        strconv::append_int(buf.clone(), Arc::new(Mutex::new(Some({ let __selector_holder = self.exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i64))), 10)
    }

    /// Format implements [fmt.Formatter]. It accepts all the regular
    /// formats for floating-point numbers ('b', 'e', 'E', 'f', 'F',
    /// 'g', 'G', 'x') as well as 'p' and 'v'. See (*Float).Text for the
    /// interpretation of 'p'. The 'v' format is handled like 'g'.
    /// Format also supports specification of the minimum precision
    /// in digits, the output field width, as well as the format flags
    /// '+' and ' ' for sign control, '0' for space or zero padding,
    /// and '-' for left or right justification. See the fmt package
    /// for details.
    pub fn format(&mut self, s: Arc<Mutex<Option<fmt_State>>>, mut format: Arc<Mutex<Option<i32>>>) {
        let (mut prec, mut hasPrec) = (*s.lock().unwrap().as_ref().unwrap()).precision();
        if !hasPrec {
        { let new_val = 6; prec = new_val; };
    }
                // default precision for 'e', 'f'
        {
        let _switch_val = { let __v = (*format.lock().unwrap().as_ref().unwrap()).clone(); __v };
        let mut _fallthrough = false;
        let mut _matched = false;
        if !_matched && (_switch_val == ('e' as i32) || _switch_val == ('E' as i32) || _switch_val == ('f' as i32) || _switch_val == ('b' as i32) || _switch_val == ('p' as i32) || _switch_val == ('x' as i32)) || _fallthrough {
            _matched = true;
            _fallthrough = false;
        }
        if !_matched && (_switch_val == ('F' as i32)) || _fallthrough {
            _matched = true;
            _fallthrough = false;
                        // (*Float).Text doesn't support 'F'; handle like 'f'
            { let new_val = ('f' as i32); *format.lock().unwrap() = Some(new_val); };
        }
        if !_matched && (_switch_val == ('v' as i32)) || _fallthrough {
            _matched = true;
            _fallthrough = false;
                        // handle like 'g'
            { let new_val = ('g' as i32); *format.lock().unwrap() = Some(new_val); };
            _fallthrough = true;
        }
        if !_matched && (_switch_val == ('g' as i32) || _switch_val == ('G' as i32)) || _fallthrough {
            _matched = true;
            _fallthrough = false;
            if !hasPrec {
        { let new_val = -1; prec = new_val; };
    }
        }
        if !_matched || _fallthrough {
            _matched = true;
            _fallthrough = false;
            { let __s = format!("%!{}(*big.Float={})", ({ let __v = (*format.lock().unwrap().as_ref().unwrap()).clone(); __v }) as u8 as char, (*self.string().lock().unwrap().as_ref().unwrap())); (*s.lock().unwrap().as_mut().unwrap()).write(Arc::new(Mutex::new(Some::<Vec<u8>>(__s.into_bytes())))) };
            return;
        }
    }
                // nothing to do
                // (*Float).Text doesn't support 'F'; handle like 'f'
                // handle like 'g'
                // default precision for 'g', 'G'
        let mut buf: Arc<Mutex<Option<Vec<u8>>>> = Arc::new(Mutex::new(None));
        { let new_val = self.append(buf.clone(), Arc::new(Mutex::new(Some((*format.lock().unwrap().as_ref().unwrap()) as u8))), Arc::new(Mutex::new(Some(prec)))); buf = new_val; };
        if { let __tmp_x = ((*buf.lock().unwrap()).as_ref().map(|__v| __v.len()).unwrap_or(0) as i32); let __tmp_y = 0; __tmp_x == __tmp_y } {
        { let new_val = Arc::new(Mutex::new(Some(("?".to_string()).as_bytes().to_vec()))); buf = new_val; };
    }
                // should never happen, but don't crash
                // len(buf) > 0
        let mut sign: Arc<Mutex<Option<String>>> = Arc::new(Mutex::new(Some(String::new())));
        if { let __tmp_x = { let __seq = { let __seq_holder = buf.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(0) as usize].clone() }; let __tmp_y = ('-' as i32) as u8; __tmp_x == __tmp_y } {
            { let new_val = "-".to_string(); *sign.lock().unwrap() = Some(new_val); };
            { let new_val = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = buf.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(1) as usize..].to_vec() }))); buf = new_val; };
        } else if { let __tmp_x = { let __seq = { let __seq_holder = buf.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(0) as usize].clone() }; let __tmp_y = ('+' as i32) as u8; __tmp_x == __tmp_y } {
                        // +Inf
            { let new_val = "+".to_string(); *sign.lock().unwrap() = Some(new_val); };
            if (*s.lock().unwrap().as_ref().unwrap()).flag((' ' as i32)) {
        { let new_val = " ".to_string(); *sign.lock().unwrap() = Some(new_val); };
    }
            { let new_val = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = buf.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(1) as usize..].to_vec() }))); buf = new_val; };
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag(('+' as i32)) {
            { let new_val = "+".to_string(); *sign.lock().unwrap() = Some(new_val); };
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag((' ' as i32)) {
            { let new_val = " ".to_string(); *sign.lock().unwrap() = Some(new_val); };
        }
                // +Inf
        let mut padding: Arc<Mutex<Option<i32>>> = Arc::new(Mutex::new(Some(0)));
        {
        let (mut width, mut hasWidth) = (*s.lock().unwrap().as_ref().unwrap()).width();;
        if hasWidth && { let __tmp_x = (width as i32); let __tmp_y = ({ let __tmp_x = ((*sign.lock().unwrap().as_ref().unwrap()).len() as i32); let __tmp_y = ((*buf.lock().unwrap()).as_ref().map(|__v| __v.len()).unwrap_or(0) as i32); __tmp_x + __tmp_y } as i32); __tmp_x > __tmp_y } {
            { let new_val = { let __tmp_x = ({ let __tmp_x = (width as i32); let __tmp_y = ((*sign.lock().unwrap().as_ref().unwrap()).len() as i32); __tmp_x - __tmp_y } as i32); let __tmp_y = ((*buf.lock().unwrap()).as_ref().map(|__v| __v.len()).unwrap_or(0) as i32); __tmp_x - __tmp_y }; *padding.lock().unwrap() = Some(new_val); };;
        }
    }
        if (*s.lock().unwrap().as_ref().unwrap()).flag(('0' as i32)) && !self.is_inf() {
                        // 0-padding on left
            write_multiple(s.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = sign.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(1))));
            write_multiple(s.clone(), Arc::new(Mutex::new(Some("0".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = padding.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
            (*s.lock().unwrap().as_ref().unwrap()).write(buf.clone());
        } else if (*s.lock().unwrap().as_ref().unwrap()).flag(('-' as i32)) {
                        // padding on right
            write_multiple(s.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = sign.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(1))));
            (*s.lock().unwrap().as_ref().unwrap()).write(buf.clone());
            write_multiple(s.clone(), Arc::new(Mutex::new(Some(" ".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = padding.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
        } else {
                        // padding on left
            write_multiple(s.clone(), Arc::new(Mutex::new(Some(" ".to_string()))), Arc::new(Mutex::new(Some({ let __arg_holder = padding.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));
            write_multiple(s.clone(), Arc::new(Mutex::new(Some({ let __arg_holder = sign.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))), Arc::new(Mutex::new(Some(1))));
            (*s.lock().unwrap().as_ref().unwrap()).write(buf.clone());
        }
    }
}

pub fn round_shortest(d: Arc<Mutex<Option<decimal>>>, x: Arc<Mutex<Option<Float>>>) {
        // if the mantissa is zero, the number is zero - stop now
    if { let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = 0; __tmp_x == __tmp_y } {
        return;
    }

        // Approach: All numbers in the interval [x - 1/2ulp, x + 1/2ulp]
        // (possibly exclusive) round to x for the given precision of x.
        // Compute the lower and upper bound in decimal form and find the
        // shortest decimal number d such that lower <= d <= upper.
        // TODO(gri) strconv/ftoa.do describes a shortcut in some cases.
        // See if we can use it (in adjusted form) here as well.
        // 1) Compute normalized mantissa mant and exponent exp for x such
        // that the lsb of mant corresponds to 1/2 ulp for the precision of
        // x (i.e., for mant we want x.prec + 1 bits).
    let mut mant = crate::nat::nat(Arc::new(Mutex::new(None::<Vec<crate::arith::Word>>))).set({ let __field = (*x.lock().unwrap().as_ref().unwrap()).mant.clone(); __field });
    let mut exp = Arc::new(Mutex::new(Some({ let __tmp_x = (*Arc::new(Mutex::new(Some({ let __selector_holder = (*x.lock().unwrap().as_ref().unwrap()).exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i32))).lock().unwrap().as_ref().unwrap()); let __tmp_y = (*mant.lock().unwrap().as_ref().unwrap()).bit_len(); __tmp_x - __tmp_y })));
    let mut s = Arc::new(Mutex::new(Some({ let __tmp_x = (*mant.lock().unwrap().as_ref().unwrap()).bit_len(); let __tmp_y = (*Arc::new(Mutex::new(Some(({ let __tmp_x = (*{ let __field = (*x.lock().unwrap().as_ref().unwrap()).prec.clone(); __field }.lock().unwrap().as_ref().unwrap()); let __tmp_y = 1 as u32; __tmp_x + __tmp_y }) as i32))).lock().unwrap().as_ref().unwrap()); __tmp_x - __tmp_y })));
    if { let __tmp_x = { let __v = (*s.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x < __tmp_y } {
            { let new_val = (*mant.lock().unwrap().as_ref().unwrap()).shl(mant.clone(), Arc::new(Mutex::new(Some(-((*s.lock().unwrap().as_ref().unwrap())) as u64)))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *mant.lock().unwrap() = __moved_val; };
        } else if { let __tmp_x = { let __v = (*s.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
            { let new_val = (*mant.lock().unwrap().as_ref().unwrap()).shr(mant.clone(), Arc::new(Mutex::new(Some({ let __v = (*s.lock().unwrap().as_ref().unwrap()).clone(); __v } as u64)))); let __moved_val = { let mut __guard = new_val.lock().unwrap(); __guard.take() }; *mant.lock().unwrap() = __moved_val; };
        }
    { let __rhs = (*s.lock().unwrap().as_ref().unwrap()); let mut guard = exp.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + __rhs); };

        // x = mant * 2**exp with lsb(mant) == 1/2 ulp of x.prec
        // 2) Compute lower bound by subtracting 1/2 ulp.
    let mut lower: Arc<Mutex<Option<decimal>>> = Arc::new(Mutex::new(Some(Default::default())));
    let mut tmp: Arc<Mutex<Option<nat>>> = Arc::new(Mutex::new(Some(Default::default())));
    (*lower.lock().unwrap().as_mut().unwrap()).init((*tmp.lock().unwrap().as_ref().unwrap()).sub(mant.clone(), natOne.clone()), Arc::new(Mutex::new(Some({ let __arg_holder = exp.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));

        // 3) Compute upper bound by adding 1/2 ulp.
    let mut upper: Arc<Mutex<Option<decimal>>> = Arc::new(Mutex::new(Some(Default::default())));
    (*upper.lock().unwrap().as_mut().unwrap()).init((*tmp.lock().unwrap().as_ref().unwrap()).add(mant.clone(), natOne.clone()), Arc::new(Mutex::new(Some({ let __arg_holder = exp.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }))));

        // The upper and lower bounds are possible outputs only if
        // the original mantissa is even, so that ToNearestEven rounding
        // would round to the original mantissa and not the neighbors.
    let mut inclusive = Arc::new(Mutex::new(Some({ let __tmp_x = crate::arith::Word(Arc::new(Mutex::new(Some(((*{ let __seq_holder = { let __named_slice = (*mant.lock().unwrap().as_ref().unwrap()).0.clone(); __named_slice }; let __seq_guard = __seq_holder.lock().unwrap(); let __seq = __seq_guard.as_ref().unwrap(); __seq[(0) as usize].clone() }.0.lock().unwrap().as_ref().unwrap()) & 2))))); let __tmp_y = crate::arith::Word(Arc::new(Mutex::new(Some(0 as u64)))); __tmp_x == __tmp_y })));

        // Now we can figure out the minimum number of digits required.
        // Walk along until d has distinguished itself from upper and lower.
    { let __range_holder = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); let __range_guard = __range_holder.lock().unwrap(); let __range_values = __range_guard.as_ref().cloned().unwrap_or_default(); drop(__range_guard); for (i, m) in __range_values.iter().copied().enumerate() {
        let mut l = (*lower.lock().unwrap().as_ref().unwrap()).at(Arc::new(Mutex::new(Some(i as i32))));
        let mut u = (*upper.lock().unwrap().as_ref().unwrap()).at(Arc::new(Mutex::new(Some(i as i32))));
                // Okay to round down (truncate) if lower has a different digit
                // or if lower is inclusive and is exactly the result of rounding
                // down (i.e., and we have reached the final digit of lower).
        let mut okdown = Arc::new(Mutex::new(Some({ let __tmp_x = l; let __tmp_y = m; __tmp_x != __tmp_y } || { let __v = (*inclusive.lock().unwrap().as_ref().unwrap()).clone(); __v } && { let __tmp_x = ({ let __tmp_x = i as i32; let __tmp_y = 1; __tmp_x + __tmp_y } as i32); let __tmp_y = (({ let __len_target = { let __field = (*lower.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); __tmp_x == __tmp_y })));
                // Okay to round up if upper has a different digit and either upper
                // is inclusive or upper is bigger than the result of rounding up.
        let mut okup = Arc::new(Mutex::new(Some({ let __tmp_x = m; let __tmp_y = u; __tmp_x != __tmp_y } && ({ let __v = (*inclusive.lock().unwrap().as_ref().unwrap()).clone(); __v } || { let __tmp_x = { let __tmp_x = m; let __tmp_y = 1 as u8; __tmp_x + __tmp_y }; let __tmp_y = u; __tmp_x < __tmp_y } || { let __tmp_x = ({ let __tmp_x = i as i32; let __tmp_y = 1; __tmp_x + __tmp_y } as i32); let __tmp_y = (({ let __len_target = { let __field = (*upper.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); __tmp_x < __tmp_y }))));
                // If it's okay to do either, then round to the nearest one.
                // If it's okay to do only one, do it.
        if { let __v = (*okdown.lock().unwrap().as_ref().unwrap()).clone(); __v } && { let __v = (*okup.lock().unwrap().as_ref().unwrap()).clone(); __v } {
            { let __recv = d.clone(); let __recv_ptr: *mut crate::decimal::decimal = { let mut __recv_guard = __recv.lock().unwrap(); __recv_guard.as_mut().unwrap() as *mut crate::decimal::decimal }; let __result = unsafe { &mut *__recv_ptr }.round(Arc::new(Mutex::new(Some({ let __tmp_x = i as i32; let __tmp_y = 1; __tmp_x + __tmp_y })))); __result };
            return;
        } else if { let __v = (*okdown.lock().unwrap().as_ref().unwrap()).clone(); __v } {
            { let __recv = d.clone(); let __recv_ptr: *mut crate::decimal::decimal = { let mut __recv_guard = __recv.lock().unwrap(); __recv_guard.as_mut().unwrap() as *mut crate::decimal::decimal }; let __result = unsafe { &mut *__recv_ptr }.round_down(Arc::new(Mutex::new(Some({ let __tmp_x = i as i32; let __tmp_y = 1; __tmp_x + __tmp_y })))); __result };
            return;
        } else if { let __v = (*okup.lock().unwrap().as_ref().unwrap()).clone(); __v } {
            { let __recv = d.clone(); let __recv_ptr: *mut crate::decimal::decimal = { let mut __recv_guard = __recv.lock().unwrap(); __recv_guard.as_mut().unwrap() as *mut crate::decimal::decimal }; let __result = unsafe { &mut *__recv_ptr }.round_up(Arc::new(Mutex::new(Some({ let __tmp_x = i as i32; let __tmp_y = 1; __tmp_x + __tmp_y })))); __result };
            return;
        }
    } }
}

/// %e: d.ddddde±dd
pub fn fmt_e(mut buf: Arc<Mutex<Option<Vec<u8>>>>, fmt: Arc<Mutex<Option<u8>>>, prec: Arc<Mutex<Option<i32>>>, d: Arc<Mutex<Option<decimal>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        // first digit
    let mut ch = Arc::new(Mutex::new(Some(('0' as i32) as u8)));
    if { let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let new_val = { let __seq = { let __seq_holder = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[(0) as usize].clone() }; *ch.lock().unwrap() = Some(new_val); };
    }
    { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push((*ch.lock().unwrap().as_ref().unwrap()).clone()); __append_target.clone() }; buf = new_val; };

        // .moredigits
    if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('.' as i32) as u8); __append_target.clone() }; buf = new_val; };
        let mut i = Arc::new(Mutex::new(Some(1)));
        let mut m = Arc::new(Mutex::new(Some(std::cmp::min((({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32), ({ let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 1; __tmp_x + __tmp_y } as i32)))));
        if { let __tmp_x = { let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*m.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[({ let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize..({ let __v = (*m.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize].to_vec() }))).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }; buf = new_val; };
        { let new_val = m.lock().unwrap().as_ref().unwrap().clone(); *i.lock().unwrap() = Some(new_val); };
    }
        while { let __tmp_x = { let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x <= __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
        { let mut guard = i.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
    }
    }

        // e±
    { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push((*fmt.lock().unwrap().as_ref().unwrap()).clone()); __append_target.clone() }; buf = new_val; };
    let mut exp: Arc<Mutex<Option<i64>>> = Arc::new(Mutex::new(Some(0)));
    if { let __tmp_x = (({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32); let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let new_val = { let __tmp_x = (*Arc::new(Mutex::new(Some({ let __selector_holder = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); let __selector_guard = __selector_holder.lock().unwrap(); let __cloned = (*__selector_guard.as_ref().unwrap()).clone(); drop(__selector_guard); __cloned } as i64))).lock().unwrap().as_ref().unwrap()); let __tmp_y = 1 as i64; __tmp_x - __tmp_y }; *exp.lock().unwrap() = Some(new_val); };
    }
        // -1 because first digit was printed before '.'
    if { let __tmp_x = { let __v = (*exp.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0 as i64; __tmp_x < __tmp_y } {
        { let new_val = ('-' as i32) as u8; *ch.lock().unwrap() = Some(new_val); };
        { let new_val = -((*exp.lock().unwrap().as_ref().unwrap())); *exp.lock().unwrap() = Some(new_val); };
    } else {
        { let new_val = ('+' as i32) as u8; *ch.lock().unwrap() = Some(new_val); };
    }
    { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push((*ch.lock().unwrap().as_ref().unwrap()).clone()); __append_target.clone() }; buf = new_val; };

        // dd...d
    if { let __tmp_x = { let __v = (*exp.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 10 as i64; __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }
        // at least 2 exponent digits
    return strconv::append_int(buf.clone(), { let __arg_holder = exp.clone(); let __arg_guard = __arg_holder.lock().unwrap(); (*__arg_guard.as_ref().unwrap()).clone() }, 10);
}

/// %f: ddddddd.ddddd
pub fn fmt_f(mut buf: Arc<Mutex<Option<Vec<u8>>>>, prec: Arc<Mutex<Option<i32>>>, d: Arc<Mutex<Option<decimal>>>) -> Arc<Mutex<Option<Vec<u8>>>> {
        // integer, padded with zeros as needed
    if { let __tmp_x = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); let __tmp_y = 0; __tmp_x > __tmp_y } {
        let mut m = Arc::new(Mutex::new(Some(std::cmp::min((({ let __len_target = { let __field = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); __field }; let __len_guard = __len_target.lock().unwrap(); __len_guard.as_ref().map(|__v| __v.len()).unwrap_or(0) }) as i32), ((*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()) as i32)))));
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).extend({ let __slice_holder = Arc::new(Mutex::new(Some({ let __seq = { let __seq_holder = (*d.lock().unwrap().as_ref().unwrap()).mant.clone(); let __seq_guard = __seq_holder.lock().unwrap(); let __cloned = __seq_guard.as_ref().cloned().unwrap_or_default(); drop(__seq_guard); __cloned }; __seq[..({ let __v = (*m.lock().unwrap().as_ref().unwrap()).clone(); __v }) as usize].to_vec() }))).clone(); let __slice_guard = __slice_holder.lock().unwrap(); __slice_guard.as_ref().map(|__v| __v.clone()).unwrap_or_default() }.iter().cloned()); __append_target.clone() }; buf = new_val; };
        while { let __tmp_x = { let __v = (*m.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
        { let mut guard = m.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
    }
    } else {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('0' as i32) as u8); __append_target.clone() }; buf = new_val; };
    }

        // fraction
    if { let __tmp_x = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = 0; __tmp_x > __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push(('.' as i32) as u8); __append_target.clone() }; buf = new_val; };
        let mut i = Arc::new(Mutex::new(Some(0)));
    while { let __tmp_x = { let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }; let __tmp_y = { let __v = (*prec.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x < __tmp_y } {
        { let new_val = { let __append_target = buf.clone(); (*__append_target.lock().unwrap()).get_or_insert_with(Vec::new).push((*d.lock().unwrap().as_ref().unwrap()).at(Arc::new(Mutex::new(Some({ let __tmp_x = (*{ let __field = (*d.lock().unwrap().as_ref().unwrap()).exp.clone(); __field }.lock().unwrap().as_ref().unwrap()); let __tmp_y = { let __v = (*i.lock().unwrap().as_ref().unwrap()).clone(); __v }; __tmp_x + __tmp_y }))))); __append_target.clone() }; buf = new_val; };
        { let mut guard = i.lock().unwrap(); *guard = Some(guard.as_ref().unwrap() + 1); }
    }
    }

    return buf.clone();
}

pub(crate) fn __go_init_functions() {
}


pub(crate) fn __go_init_all() {
}
